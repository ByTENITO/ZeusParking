<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Estado cuando el RadioButton está seleccionado -->
    <item android:drawable="@drawable/disable" android:state_checked="true"/>

    <!-- Estado cuando el RadioButton NO está seleccionado -->
    <item android:drawable="@drawable/availability" android:state_checked="false"/>

</selector>