<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Estado cuando el RadioButton está seleccionado -->
    <item android:drawable="@drawable/iconoavailability" android:state_checked="false" android:buttonTint="@null"/>

    <!-- Estado cuando el RadioButton NO está seleccionado -->
    <item android:drawable="@drawable/iconodisabled" android:state_checked="true" android:buttonTint="@null"/>

</selector>