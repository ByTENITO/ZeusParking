<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Permiso para acceso a Internet -->
    <uses-permission android:name="android.permission.INTERNET" /> <!-- Permiso para comprobar el estado de la red -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher_foreground"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ParquiatenoV10"
        tools:targetApi="31">
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyAE4ILo36Dc-bwocj7dJzekPvRo7UQexQc" />

        <activity
            android:name=".Localizacion"
            android:exported="false"
            android:theme="@style/Theme.ParquiatenoV10" />
        <activity
            android:name=".Disponibilidad"
            android:exported="false" />
        <activity
            android:name=".EntradaQrActivity"
            android:exported="false" />
        <activity
            android:name=".RegistrarBiciActivity"
            android:exported="false" /> <!-- HomeActivity no debe ser exportada si no tiene que ser accesible desde otras aplicaciones -->
        <activity
            android:name=".HomeActivity"
            android:exported="false" /> <!-- AuthActivity es la actividad principal, por eso es exportada y tiene el intent-filter -->
        <activity
            android:name=".AuthActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>